import { ConfigInterface } from './interfaces/config.interface';

export const CaroConfig: ConfigInterface = {
  players: 2,
  size: 15,
  winCondition: 5,
  playerSymbols: ['X', 'O'],
  timeLimit: 30,
};
